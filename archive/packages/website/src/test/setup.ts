import { beforeAll, afterEach, afterAll } from 'vitest';

// Setup for DOM testing
beforeAll(() => {
  // Global test setup
});

afterEach(() => {
  // Cleanup after each test
});

afterAll(() => {
  // Global cleanup
});