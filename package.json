{
  "name": "uepm-monorepo",
  "version": "1.0.4",
  "private": true,
  "description": "Unreal Engine Package Manager - Monorepo",
  "workspaces": [
    "packages/*",
    "samples/plugins/*",
    "samples/tests"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "clean": "npm run clean --workspaces --if-present && rm -rf node_modules"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.3",
    "vitest": "^1.1.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "keywords": [
    "unreal",
    "unreal-engine",
    "plugin",
    "package-manager",
    "monorepo"
  ],
  "author": "",
  "license": "MIT"
}
