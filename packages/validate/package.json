{
  "name": "@uepm/validate",
  "version": "0.1.1",
  "description": "Validation hook for checking Unreal Engine plugin compatibility",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "uepm-validate": "./bin/uepm-validate.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "vitest --run",
    "test:watch": "vitest",
    "clean": "rm -rf dist",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "unreal",
    "unreal-engine",
    "plugin",
    "uepm",
    "validation"
  ],
  "author": "",
  "license": "MIT",
  "files": [
    "dist",
    "bin",
    "README.md"
  ],
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@types/semver": "^7.5.6",
    "fast-check": "^3.15.0"
  },
  "dependencies": {
    "@uepm/core": "^0.1.2",
    "semver": "^7.5.4"
  }
}
